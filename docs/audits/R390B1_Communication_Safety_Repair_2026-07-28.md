# R390B1 Communication Safety Repair

Release track: `Track H`
Baseline: `2b5273a7eb178872eb8be05d638b491e20d78404`
Target: Admin `@426 / r390 / 390`; Student unchanged `@247 / r217 / 217`; Production untouched.

## Read-only reconciliation

The same live `FODE_Data` population was read before and after applying the source projection locally. No Sheet row was changed.

| Measure | Prior audit / live raw | R390B1 source projection | Reconciliation |
|---|---:|---:|---|
| Population | 335 | 335 | Unchanged |
| False duplicate suppression | 7 | 0 | Seven legacy `SUPPRESSED` rows project from successful-send evidence instead of contactability failure |
| Valid cooldown | 54 | 54 | Preserved |
| Valid contactability block | 67 | 67 | Preserved: 65 without a valid effective email and 2 with genuine failed delivery/contact evidence |
| Valid authority block | 11 | 11 | Prior audited authority total preserved; authority blockers retain precedence over attempt-three manual review |

The seven recovered compatibility rows are:

- `FODE-26-002972`
- `FODE-26-002976`
- `FODE-26-002980`
- `FODE-26-002987`
- `FODE-26-003013`
- `FODE-26-003020`
- `FODE-26-003021`

Each has successful-send timestamps, `Email_Attempt_Count = 2`, no bounce evidence, and an expired next-action date. R390B1 therefore removes false contactability/suppression meaning and allows the genuine attempt-three `MANUAL_REVIEW_REQUIRED` projection after higher-priority contactability and lifecycle authority checks.

## Safety result

- Preview allocates and retains `operationId`, `previewId`, and `receiptId`.
- Execute validates and consumes the approved identity.
- Individual and selected-batch dispatch receive the same identity.
- Exact retry returns the original cached receipt where available and does not dispatch or rewrite row evidence.
- Later eligible follow-up receives a new operation identity.
- Successful sends alone increment `Email_Attempt_Count`.
- First successful send retains a 2-day next-action cadence; second retains 4 days; attempt three requires manual review.
- `SUPPRESSED` is no longer written by the repaired idempotency path or interpreted as contactability failure.
- Receipts preserve full blocker, identity, delivery evidence, ISO time, and PNG-local time.
- Receipt history renders separately labelled fields.
- Idempotency and receipt history remain explicitly transient User Cache authority; durable cross-cache idempotency is deferred.
- Stage Batch source is unchanged.

## Validation

- Changed runtime `node --check`: PASS.
- Focused R390B1 communication safety test: PASS.
- Directly affected communication, actionability, selected-batch, EduOps, Finance, lifecycle, portal, and deployment-contract tests: PASS.
- Full repository Node suite: PASS before R390B1C. Not rerun after R390B1C because the mandatory Preview Lab browser gate failed first.
- Deployable-file contract: PASS before R390B1C. Not rerun after R390B1C because the mandatory Preview Lab browser gate failed first.
- Preview Lab fixture syntax and contract: PASS after the R390B1A-authorized fixture repair added canonical access, profile, and `EDUOPS_WORKLOAD_PRESENTATION_V1` DTOs.
- Preview Lab browser suite: the original navigation failure is repaired (`7` expected actionability navigation buttons now render), but the suite stops at `preview-lab.browser.test.js:40`: the unchanged production client renders `8` KPI buttons from the canonical actionability buckets while the unchanged test requires `0`.
- R390B1B investigation found that the KPI buttons were intentionally introduced historically as state-navigation controls, but the current accepted `EduOps.html` permanently hides the entire KPI surface with `hidden` and exposes no disclosure control. The current browser assertion therefore protects the accepted no-duplicate-control surface; changing it would normalize hidden production controls rather than reconcile an authoritative visible contract. The authorized browser test was restored unchanged. Release work stopped before the Config bump, Apps Script push, version creation, or deployment repin.
- R390B1C removed the obsolete hidden KPI rendering path in `EduOps_ClientComponents.html`: `renderKpis(presentation)` no longer creates eight `eduops-kpi-card` / `data-state-summary` buttons, `app.renderWorkload` now clears the legacy container, and the hidden-only `data-state-summary` click branch was removed. The visible seven-state `#eduopsActionNav` and history `#eduopsHistoryNav` `data-state` navigation paths remain intact.
- R390B1C source checks confirm the legacy `#eduopsKpis` container remains under `EduOps.html` `<details class="eduops-overview eduops-operations-secondary-surface" hidden>`, and `preview-lab.browser.test.js` remains identical to `HEAD`.
- R390B1C Preview Lab browser suite now passes the seven actionability-state and zero legacy KPI-button assertions, then stops at `preview-lab.browser.test.js:43` because `#eduopsGlobalSearch` computed `font-size` is below the required `15px`. Source trace: `EduOps_Styles.html` sets `.eduops-global-search-strip .eduops-global-search input { font-size: 14px; }`. That stylesheet is outside the R390B1C edit allowlist, so release work stopped before Config bump, Apps Script push, version creation, or deployment repin.
- R390B1D confirmed the active font cascade in the rendered Preview Lab browser: `#eduopsGlobalSearch` computed `14px` from `.eduops-global-search-strip .eduops-global-search input { font-size: 14px; }`, overriding broader accepted `15px` input rules. `EduOps_Styles.html` was corrected to `15px`; targeted browser proof then reported `font: 15px`, `nav: 7`, `kpi: 0`.
- R390B1D also repaired latent Preview Lab fixture DTO gaps uncovered after the font assertion passed: deterministic search results now include canonical presentation/search-handoff data, exact applicant Workbench responses include `EDUOPS_APPLICANT_WORKBENCH_V2` and required tab sub-schemas, and document manifests include `EDUOPS_DOCUMENT_MANIFEST_V2` / `OPSEDU_DOCUMENT_GALLERY_V1`. `EduOps_ClientWorkbench.html` document preview wording was aligned with the accepted browser contract: "The canonical original is unchanged."
- R390B1D Preview Lab browser suite now progresses to `preview-lab.browser.test.js:100` and stops because `#eduopsStartSession` is not visible. Source trace: `EduOps.html` places the button inside `<details class="eduops-overview eduops-operations-secondary-surface" hidden>`, the same permanently hidden legacy summary surface identified during R390B1B/R390B1C. `EduOps.html` is not authorized for edit by R390B1D, and re-enabling or relocating hidden workspace controls would broaden the release beyond the CIS. Release work stopped before Config bump, Apps Script push, version creation, or deployment repin.
- R390B1E determined that `#eduopsStartSession` remains authoritative. No current visible replacement control performs the same action, session initialization is not automatic, and `EduOps_ClientBatch.html` binds `openWorkSession()` only to `#eduopsStartSession`. That function builds the session from the current visible workload rows, opens the first applicant Workbench, and renders `#eduopsSessionBar`; the Workbench module re-renders the active session when workbench data loads. The Preview Lab and owner-proxy browser tests therefore validate a real Work Session operator workflow, not obsolete legacy markup.
- R390B1E used Path A. The single existing `#eduopsStartSession` control was moved from the permanently hidden legacy workload summary into the visible `EduOps.html` `.eduops-operations-queue-toolbar` next to the existing work-package control. The identifier, label, type, and event wiring were preserved. The hidden legacy summary surface remains hidden, no duplicate session-start button remains, and no legacy KPI controls were restored.
- R390B1E also repaired the fixture/session-return prerequisites surfaced immediately after the session control became clickable: deterministic workload rows now include `EDUOPS_ROW_AUTHORITY_DECISION_V1`, fixture access/profile/workload/workbench DTOs expose `BATCH_COMMUNICATION` operation availability, and `restoreReturnContext()` no longer clears and reloads an unchanged current workload when returning from Work Session.
- R390B1E Preview Lab browser suite now passes the previous session-start visibility/click path, opens the Work Session, advances the exact applicant, exits to workload, enables Batch Operations after selecting visible rows, and opens `#eduopsBatchWorkspace`. The suite then stops at `preview-lab.browser.test.js:115` because it expects the retired selector `input[name="eduopsBatchOperation"][value="BATCH_COMMUNICATION"]`. Current `EduOps_ClientBatch.html` uses a fixed `BATCH_COMMUNICATION` operation with explicit execution-limit and communication selectors; no operation radio is rendered. This is a new browser-contract reconciliation blocker outside the R390B1E session-control edit authorization. Release work stopped before Config bump, Apps Script push, version creation, deployment repin, staging, commit, or Git push.
- R390B1F source and history confirm the fixed-operation design is authoritative. `EduOps_ClientBatch.html` assigns `app.state.batch.operation = "BATCH_COMMUNICATION"` when the workspace opens, requests the communication catalogue with that operation, and passes the same state into `eduops_previewCommand`; `EduOps_Commands.js` resolves the command definition and dispatches the selected batch under `BATCH_COMMUNICATION`. No active source outside the stale browser assertion depends on `name="eduopsBatchOperation"`.
- Historical commit `cea6894` (`fix: enforce backend authority across EduOps work surface`) intentionally removed `operationOptions()` and the single `eduopsBatchOperation` change handler while adding server-authored execution-limit and communication/template selection. Because Batch Communication was the only proven batch-safe authority, operators were no longer presented with a false choice between operation types.
- R390B1F replaced the obsolete radio interaction with behavioural proof: the Batch workspace opens; no retired operation radio is rendered; `EduOpsApp.state.batch.operation` is exactly `BATCH_COMMUNICATION`; the execution-limit and communication selectors are visible, enabled, and accept authoritative values; those values and the fixed operation remain present in Batch preview state; and the current preview action becomes enabled and initiates the server-authored partition workflow. No production markup was added or changed for test inspection.
- R390B1F directly affected validation exposed a wording regression from the R390B1D document clarification: the Preview Lab phrase "The canonical original is unchanged" had displaced the accepted explicit "Original document - authoritative evidence" role label. `EduOps_ClientWorkbench.html` now presents both statements beside "Browser preview - derived viewing copy"; behavior, controls, and authority are unchanged.
- The first R390B1F browser run proved the execution-limit control was usable, then timed out because the deterministic fixture advertised Batch availability but returned `UNKNOWN_RPC` for `eduops_getBatchCommunicationCatalogue`. The fixture now implements the current `EDUOPS_BATCH_COMMUNICATION_CATALOGUE_V1` DTO, preserves the explicit selection binding and execution limit, returns one server-authored communication choice and recipient projections, and supplies the current Batch preview/receipt fields. This is fixture-only normalization; production Batch source remains unchanged.
- After that fixture repair, the R390B1F browser suite passes all previously completed navigation, font, Waffi Workbench, Work Session, and Batch steps, including fixed `BATCH_COMMUNICATION`, execution-limit selection, communication selection, preview, partition, confirmation, simulated execution, and receipt. It then stops at the unrelated existing assertion `preview-lab.browser.test.js:157`: the active `reconciliation` report title is "Workload composition" from `reportMeta`, while the test still requires `/reconciliation/i`. Historical commit `cea6894` introduced the accepted title during backend-authority hardening without updating the older browser assertion. R390B1F prohibits changing unrelated browser assertions, so this is a release blocker requiring separate authorization; no Config bump or release action followed.
- R390B1G source and history confirm `Workload composition` is the accepted report title. Commit `1169fcc` deliberately introduced the title and "Open composition" operator wording; later authority-hardening commit `cea6894` retained the title while strengthening the same surface to require `EDUOPS_RECONCILIATION_RESPONSE_V1` / `EDUOPS_RECONCILIATION_V1` and render canonical population arithmetic. The operator control still opens `openReport("reconciliation")` and calls `eduops_getReconciliation`; only the public presentation title changed.
- R390B1G replaced the stale `/reconciliation/i` assertion with exact `Workload composition` equality and behavioural proof that exactly one report surface opens, the authoritative workload-composition explanation and arithmetic render, and canonical population, matched workload, visible page, and outside-current-view counts equal the active Preview Lab workload DTO. Production report source and markup were not edited.
- The first R390B1G browser run passed the exact title but exposed a canonical fixture gap: `reconciliationRpc()` returned unversioned raw counts, so the production client correctly rejected it instead of rendering report content. The fixture now returns `EDUOPS_RECONCILIATION_RESPONSE_V1` containing `EDUOPS_RECONCILIATION_V1`, canonical population arithmetic fields, query identity, and a bounded hidden-reason page matching the production response shape.
- After the reconciliation fixture repair, the primary Preview Lab browser suite passes across all three viewports: `48` behaviours per viewport, `144` assertions, zero console errors, zero page errors, and zero overflow. This proves seven Actionability states, zero legacy KPI buttons, 15px global search, Waffi Workbench, Work Session, fixed Batch Communication through receipt, exact `Workload composition` title, and canonical report counts.
- The package's separate clean-start browser test then stops at `eduops-preview-clean-start.browser.test.js:137`, waiting for retired `#eduopsSafetyDetails`. Active production markup contains no such control, while the test step dates to the original `3c544ca` preview contract. R390B1G does not authorize editing the clean-start test or restoring retired production markup. Because the CIS requires the complete Preview Lab browser suite, this remains a release blocker; full-suite and deployment gates did not begin.
- R390B1H classifies `#eduopsSafetyDetails` as a stale test contract. Commit `1169fcc` removed the interactive legacy safety banner and replaced it with the runtime release/snapshot identity surface plus the System Health support report; later Operations Workspace density placed source reliability and refresh inside the visible "Search and authority status" disclosure. The clean-start harness now proves the visible Preview Lab title states `Simulated data / no live operations`, the runtime and snapshot identities resolve, no retired control exists, the current authority-status disclosure exposes an authoritative reliability state, and refresh completes. No production compatibility control was restored.
- R390B1H classification A: the first replacement incorrectly treated the technical `#eduopsPreviewModeBanner` inside the collapsed Preview Lab body as a visible clean-start surface. The corrected harness validates the genuinely visible Preview Lab status title while retaining resolved runtime/snapshot assertions.
- R390B1H fixture reconciliation: clean-start then proved that the Preview Lab workload DTO omitted canonical `runtime`, leaving the production identity renderer at `Runtime identity unavailable.` even though the access fixture already supplied `EDUOPS_RUNTIME_IDENTITY_V1`. Deterministic workloads now reuse that authority DTO; captured-snapshot workloads reuse the captured access projection with a metadata-only identity fallback.
- R390B1H classification A: the clean-start harness next attempted the hidden legacy `#eduopsRefreshSnapshot` after already exercising the visible `#eduopsRefreshAuthority`. Both handlers request a refreshed workload, but only `Refresh source` is in the current operator surface. The harness now proves the legacy duplicate remains hidden and exercises refresh through the visible control.
- R390B1H classification A: global-search clear still waited for retired helper copy (`Search the active product`). The production component now restores the product-aware FODE prompt `Find any applicant by name, ApplicantID, email or phone.`; the harness now waits for that exact meaningful reset after proving Waffi search results.
- R390B1H classification A: selection smoke still asserted retired `N selected` copy, and its broad `/selected/i` positive check could pass on the mode label alone. The reconciled checks require a non-zero `Operator selection intent` after selecting visible rows and exact zero intent/zero exclusions after clearing.
- R390B1H harness reconciliation: `waitWorkload` required at least one table row, so valid zero-result owner/urgency filters timed out even after the request settled and the canonical empty state rendered. The wait predicate now accepts either returned rows or the visible empty-state surface while still requiring `aria-busy=false` and a non-loading range.
- R390B1H classification A: deliberate source-failure recovery asserted and targeted the reliability banner inside the default-closed authority disclosure. The current visible failure contract is the workload error state (`Workload could not be loaded`, explicit source-unavailable reason, and `Retry workload`). The harness now verifies and activates that visible retry, while confirming the hidden legacy refresh remains non-operator-facing.
- R390B1H clean-start browser suite: PASS across `1366x768`, `1440x900`, and `1920x1080`; `144` visible control interactions, zero console errors, zero page errors, zero failed requests, and zero unresolved requests. The deliberate source-unavailable retry/recovery and occupied-port fail-closed checks pass.
- R390B1H complete Preview Lab package: PASS. Contract methods `13` with simulated receipts and read-only capture; primary browser `48` behaviours x `3` viewports = `144` assertions with zero console errors, page errors, or overflow; clean-start `3` viewports / `144` visible control interactions with zero console errors, page errors, failed requests, or unresolved requests.
- R390B1H full repository suite: PASS, `75/75` Node test files. The explicit deployable Apps Script contract also passes with `46` files and set hash `8add6f15ed2f57063e164da0779de7dc539028ad06554ad45b07a45a5e8b801d`; read-only `clasp status` recognizes the deployable set.
- Release-helper follow-up (non-blocking baseline tooling defect): `tools/preflight.ps1` passes repo, Config, critical flags, syntax, `git diff --check`, and runtime URL gates, but still searches `Admin.js` for five functions modularized before baseline. HEAD proves the current owners are `Admin_ReviewStatusAuthority.js`, `Admin_SelectedApplicantCommunications.js`, and `Admin_StageBatchCommunications.js`. The release file allowlist does not authorize this unrelated tool repair; the purpose-built `fode-release-preflight.ps1` remains the release gate.
- Pre-release complete diff scope gate: PASS against `2b5273a7eb178872eb8be05d638b491e20d78404`. Exactly `26` authorized dirty paths before the Config bump, staged diff empty, generated Preview Lab evidence removed as reproducible, no protected Finance/lifecycle/Student/Production/Zoho/portal/Classroom file changes, and no PowerShell edits.
- Release identity and remote-source gates: local Config changed once from `r389 / 389` to `r390 / 390`; the single authorized `clasp push` uploaded `46` files. Three consecutive Apps Script API `projects.getContent` readbacks produced identical local/remote deployable sets (`bccc774475e66d1a1d1e20a9653013429ade48f3be525d2d58a02c1ed1c53119`), full local/remote source hash matches, and remote Config `r390 / 390`.
- Apps Script version `426` was created from the verified source and only Admin staging was repinned. Read-only live verification: Admin `@426 / r390 / 390` PASS; Student remains `@247 / r217 / 217` PASS; Production untouched.
- Post-release browser evidence: legacy Admin health PASS. The old hydration60 spec records `r390 / 390`, `336` classified applicants, populated Operations Workspace metrics, no page errors/warnings/dangerous controls, and no persistent runtime loading, but its final retired-contract assertion requires at least one legacy `Review` button and fails with zero. The legacy communication smoke likewise stops on retired top-level `Communication Performance` wording before inspecting the selected applicant. Neither external F: harness was edited.
- Current permanent Operations Workspace communication-cohort/preview browser spec exits PASS at `r390 / 390`, with no page errors or dangerous controls. It records `PASS_WITH_WARNINGS` only because the current Applicant Action selection contains zero authoritative communication cohorts, so no live batch preview could be exercised. Local deterministic Preview Lab remains the complete Batch-through-receipt browser proof; no live send occurred.
- R390B1H also aligns clean-start report interactions with accepted public titles: `Open workload composition` expects exact `Workload composition`, and the Finance report expects `Finance`. These remain behaviour-sensitive report-open checks rather than generic visibility assertions.
- `git diff --check`: PASS.

Automated validation sent no Gmail and performed no applicant, Sheet, Zoho, Classroom, portal, role, or grant mutation.
