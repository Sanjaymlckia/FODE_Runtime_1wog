<?php require __DIR__.'/bootstrap.php'; if(!in_array('--plan',$argv,true)) throw new RuntimeException('Restore verification is plan-only unless explicitly invoked by a protected operator.'); echo "PLAN: verify checksum; restore into separately configured verification database; compare table and row counts; run FK/unique checks; perform signed read/write test; never overwrite active database.\n";
