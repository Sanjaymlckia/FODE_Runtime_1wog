# FODE Communication Ledger

PHP 8.1 / MariaDB 11.4 source package for durable, signed communication authority. It is source-ready only: it neither calls Gmail nor enables Stage Batch execution. See `docs/` for deployment after independent TLS confirmation.
