# FODE Project Context

FODE is the first Runtime Engineering Platform consumer.

Authoritative configuration: `runtime-context.json`, project key `FODE`.

This folder should contain only FODE-specific onboarding notes that do not duplicate IDs, URLs, deployment pins, or evidence paths. Update the context file first, then reference the context key here.
